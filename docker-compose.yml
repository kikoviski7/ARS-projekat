version: '3.9'

services: 
  api: 
    build: .
    container_name: ars-api
    ports: 
      - "8000:8000"
    depends_on:
      - consul
    
  consul: 
    image: hashicorp/consul
    container_name: consul
    ports: 
     - "8500:8500"